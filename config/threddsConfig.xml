<?xml version="1.0" encoding="UTF-8"?>
<threddsConfig>

  <!-- all options are commented out in standard install - meaning use default values -->
  <!-- see http://www.unidata.ucar.edu/projects/THREDDS/tech/reference/ThreddsConfigXMLFile.html -->
  <serverInformation>
    <name>IMOS Thredds Server</name>
    <logoUrl>https://imos.org.au/wp-content/uploads/2024/06/Imos-horizontal-logo-optimised@x2.svg</logoUrl>
    <logoAltText>IMOS Thredds Server</logoAltText>

    <abstract>Integrated national array of observing equipment to monitor physical, chemical and
      biological variables open oceans and coastal marine environment around Australia</abstract>
    <keywords>multi-decadal ocean change climate variability weather extremes major boundary
      currents interbasin flows continental shelf processes ecosystem responses</keywords>

    <contact>
      <name>General Enquiries</name>
      <organization>IMOS</organization>
      <email>info@aodn.org.au</email>
      <phone>+61 (03) 6226 7488</phone>
    </contact>
    <hostInstitution>
      <name>IMOS</name>
      <webSite>http://imos.org.au/</webSite>
      <logoUrl>https://imos.org.au/wp-content/uploads/2024/06/Imos-horizontal-logo-optimised@x2.svg</logoUrl>
      <logoAltText>IMOS</logoAltText>
    </hostInstitution>
  </serverInformation>

  <!--
   * Setup for generated HTML pages.
   *
   * NOTE: URLs may be absolute or relative, relative URLs must be relative
   * to the webapp URL, i.e., http://server:port/thredds/.
    -->
  <htmlSetup>
    <!--
     * CSS documents used in generated HTML pages.
     * The CSS document given in the "catalogCssUrl" element is used for all pages
     * that are HTML catalog views. The CSS document given in the "standardCssUrl"
     * element is used in all other generated HTML pages.
     * -->
    <!-- <standardCssUrl>/thredds/tds.css</standardCssUrl>
    <catalogCssUrl>/thredds/tdsCat.css</catalogCssUrl>
    <openDapCssUrl>/thredds/tdsDap.css</openDapCssUrl>

    <folderIconUrl>/thredds/folder.gif</folderIconUrl> -->

    <!--
     * The Google Analytics Tracking code you would like to use for the
     * webpages associated with THREDDS. This will not track WMS or DAP
     * requests for data, only browsing the catalog.
    -->
    <googleTrackingCode></googleTrackingCode>

  </htmlSetup>

  <!--
   The <CatalogServices> element:
   - Services on local TDS served catalogs are always on.
   - Services on remote catalogs are set with the allowRemote element
   below. They are off by default (recommended).
   -->
  <CatalogServices>
    <allowRemote>false</allowRemote>
  </CatalogServices>

  <!--
  CDM uses the DiskCache directory to store temporary files, like uncompressed files.
  -->
  <DiskCache>
    <alwaysUse>true</alwaysUse>
    <scour>1 min</scour>
    <maxSize>40 Gb</maxSize>
  </DiskCache>

  <!--
  Caching open NetcdfFile objects.
  default is to allow 50 - 100 open files, cleanup every 11 minutes
  -->

  <NetcdfFileCache>
    <minFiles>200</minFiles>
    <maxFiles>400</maxFiles>
    <scour>10 min</scour>
  </NetcdfFileCache>

  <!--
  The <HTTPFileCache> element:
  allow 10 - 20 open datasets, cleanup every 17 minutes
  used by HTTP Range requests.
  -->
  <HTTPFileCache>
    <minFiles>20</minFiles>
    <maxFiles>40</maxFiles>
    <scour>10 min</scour>
  </HTTPFileCache>


  <!--
  Persist joinNew aggregations to named directory. scour every 24 hours, delete stuff older than 90
  days
  -->
  <AggregationCache>
    <scour>24 hours</scour>
    <maxAge>30 days</maxAge>
  </AggregationCache>

  <!--
  How to choose the template dataset for an aggregation. latest, random, or penultimate
  -->
  <Aggregation>
    <typicalDataset>penultimate</typicalDataset>
  </Aggregation>

  <!--
  The Netcdf Subset Service is off by default.
  -->
  <NetcdfSubsetService>
    <allow>true</allow>
    <scour>10 min</scour>
    <maxAge>-1 min</maxAge>
  </NetcdfSubsetService>

  <WMS>
    <allow>true</allow>
    <allowRemote>false</allowRemote>
    <maxImageWidth>2048</maxImageWidth>
    <maxImageHeight>2048</maxImageHeight>
  </WMS>

  <!-- CatalogGen service is off by default. -->
  <CatalogGen>
    <allow>true</allow>
  </CatalogGen>

</threddsConfig>